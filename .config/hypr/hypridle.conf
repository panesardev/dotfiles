# general {
#     lock_cmd = pidof hyprlock || hyprlock       # Avoid multiple instances
#     before_sleep_cmd = loginctl lock-session    # Lock before suspend
#     after_sleep_cmd = hyprctl dispatch dpms on  # Turn display on after wake
# }

# listener {
#     timeout = 900                                # 5 min
#     on-timeout = loginctl lock-session           # Lock when idle
# }

# listener {
#     timeout = 901                                # 5 min 1 second
#     on-timeout = hyprctl dispatch dpms off       # Turn off screen
#     on-resume = hyprctl dispatch dpms on         # Screen on when active
# }